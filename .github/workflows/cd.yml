# name: CD - AdminDashboard (Azure Container Apps)

# # ======== Trigger CD (push)
# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:

#     # ======== Checkout
#     - name: Checkout repository
#       uses: actions/checkout@v4


#     # ======== Connexion Azure
#     - name: Azure Login
#       uses: azure/login@v2
#       with:
#         creds: ${{ secrets.AZURE_CREDENTIALS }}


#     # ======== Connexion Azure Container Registry (ACR)
#     - name: Login to Azure Container Registry
#       run: |
#         az acr login --name admindashboardregistry


#     # ======== Build Docker prod (back+front)
#     - name: Build backend image
#       run: |
#         docker build \
#           -t admindashboardregistry.azurecr.io/admindashboard-api:latest \
#           -f backend/AdminDashboard.Api/Dockerfile \
#           backend

#     - name: Build frontend image
#       run: |
#         docker build \
#           -t admindashboardregistry.azurecr.io/admindashboard-frontend:latest \
#           -f frontend/Dockerfile \
#           frontend


#     # ======== Push Docker (back+front) vers ACR
#     - name: Push backend image
#       run: |
#         docker push admindashboardregistry.azurecr.io/admindashboard-api:latest

#     - name: Push frontend image
#       run: |
#         docker push admindashboardregistry.azurecr.io/admindashboard-frontend:latest


#     # ======== Déploiement (back+front) sur ACA
#     - name: Deploy backend to Azure Container Apps
#       run: |
#         az containerapp update \
#           --name admindashboard-api \
#           --resource-group admindashboard-rg \
#           --image admindashboardregistry.azurecr.io/admindashboard-api:latest

#     - name: Deploy frontend to Azure Container Apps
#       run: |
#         az containerapp update \
#           --name admindashboard-frontend \
#           --resource-group admindashboard-rg \
#           --image admindashboardregistry.azurecr.io/admindashboard-frontend:latest


#     # ======== Attente que l'API REST soit prête
#     - name: API Health Check
#       run: |
#         echo "Attente de l'API REST..."
#         for i in {1..20}; do
#           if curl -s https://admindashboard-api.azurecontainerapps.io/api/Products > /dev/null; then
#             echo "API REST OK"
#             exit 0
#           fi
#           sleep 5
#         done

#         if [ $i -eq 20 ]; then
#           echo "API REST FAILED"
#           exit 1
#         fi


#     # ======== Attente que GraphQL soit prêt
#     - name: API GraphQL Health Check
#       run: |
#         echo "Attente GraphQL..."
#         for i in {1..20}; do
#           if curl -s -X POST https://admindashboard-api.azurecontainerapps.io/graphql \
#             -H "Content-Type: application/json" \
#             -d '{"query":"{ __typename }"}' | grep "__typename"; then
#             echo "GraphQL OK"
#             exit 0
#           fi
#           sleep 5
#         done

#         echo "GraphQL FAILED"
#         exit 1


#     # ======== Message de fin du pipeline
#     - name: Deployment finished
#       run: echo "CD réussi"